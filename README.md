# Pizza Slice Party Splitter

## Run locally

1. Install dependencies
```bash
npm install
```
2. Start server
```bash
node server.js
```
3. Open browser at `http://localhost:3000`

The app will load the SPA, let you add people, enter total cost and slices, and see real‑time share calculations.
